---
title: ""   # optional; falls back to the project's label in src/lib/projects.ts
date: 2026-07-27
dek: ""
# description: ""   # optional; the meta description. Falls back to dek when blank
# updated: 2026-07-27   # optional; drives dateModified + the visible freshness line
---

<!--
  PROJECT PAGE TEMPLATE -- how to use (delete this whole comment before saving a real one)

  WHAT THIS IS. A Project is the entity behind every portfolio piece (see
  src/lib/projects.ts). Every piece is a project; only some get a page. THIS FILE
  IS WHAT DECIDES IT. Save `content/projects/<slug>.md` and /showroom/<slug>/
  becomes a real page, linked from the room. Delete the file and the project goes
  back to being showroom-only. There is no flag to flip anywhere else.

  1. Copy this file. Rename it to the project's SLUG exactly as it appears in
     src/lib/projects.ts, e.g. content/projects/rising-compass.md -> /showroom/rising-compass/.
     A file whose slug matches no project is ignored, deliberately: the page would
     have no capture, no live link, and nowhere in the room to be linked from.
  2. Fill title (or leave it blank to use the project's label) and dek.
  3. Write it in Google Docs, then Edit > Copy as Markdown (or File > Download >
     Markdown) and paste the markdown BELOW this comment. DO NOT plain-copy-paste;
     that loses all formatting.
  4. Do NOT paste the title as a heading in the body -- the title comes from the
     frontmatter, so an H1 in the body would double it.
  5. Delete this comment. Save. Ship with deploy.sh.

  WHAT THE PAGE ALREADY KNOWS, so do not retype it in the prose: the label, the
  live URL, the platform, the year, and the capture. Those come from the project
  entity and render around your words.

  WHAT THIS PAGE IS FOR (CWS-EXPANSION-PLAN-01 item C). A craft narrative:
  someone had a big idea, here is what got built. It is SUPPORTING evidence under
  the essays, not the headline. That framing constrains what belongs here:

  - NO invented metrics. No revenue numbers, no ranking charts, no "300% more
    leads." The plan bans manufactured results by name and the ban is the point:
    the proof is the build itself. If a number is real and measured, say when and
    how it was measured. If it is not, leave it out.
  - The proof currency is what got BUILT and what it took. Before and after,
    the constraint, the decision that was not obvious, the part that was hard.
  - No client is obliged to be flattered into a case study. Say what is true.

  A PROMPT, NOT A FORM. The same rule the essays run on applies here: AI drafts
  none of this. The questions below are a reference to write against, not blanks
  to fill and not headings to keep.

  - What did the person want that no template could hold?
  - What did you actually build, in concrete terms a non-technical reader follows?
  - What was the hard part, and what did you decide to do about it?
  - What can a visitor go and check for themselves right now?
  - What does this build prove that the other work does not?

  LENGTH: longer than an essay, shorter than a case study. Enough that a reader
  finishes knowing what was built and why it was not easy.
-->

Opening line goes here.

Body.

Closing turn.
